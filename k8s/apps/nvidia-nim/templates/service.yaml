apiVersion: v1
kind: Service
metadata:
  name: nim-llm
  labels:
    app: nim-llm
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: nim-llm
